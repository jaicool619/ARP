#include "arp.h" //including header file

// function to delay opening the admin panel after entering username & password
void fordelay(int j)
{
	int delay; // variable to store integer values:

	for (int i = 0; i < j; i++)
	{
		delay = i;
	} // end_of_for_loop:
} // end_fordelay_function:
